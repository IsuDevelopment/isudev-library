name: Build & Release

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'isudev-library.php'

permissions:
  contents: write

jobs:
  release:
    name: Build and release plugin
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read version from plugin header
        id: version
        run: |
          VERSION=$(grep -m1 'Version:' isudev-library.php | awk '{print $NF}' | tr -d '[:space:]')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Check if tag already exists
        id: tag_check
        run: |
          if git rev-parse "refs/tags/v${{ steps.version.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer:v2

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Build editor assets
        run: |
          npm ci
          npm run build

      # main stays source-only. The tag carries build/ as well, so Composer installs from this
      # repository get a runnable plugin without a build step.
      - name: Tag the release with built assets
        if: steps.tag_check.outputs.exists == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f build
          git commit -m "Build v${{ steps.version.outputs.version }}"
          git tag "v${{ steps.version.outputs.version }}"
          git push origin "v${{ steps.version.outputs.version }}"

      - name: Build plugin zip
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          mkdir -p dist/isudev-library
          rsync -a --exclude-from=- ./ dist/isudev-library/ <<'EOF'
          .git
          .github
          .agents
          dist
          node_modules
          src
          *.zip
          .gitignore
          .editorconfig
          .eslintrc.js
          .prettierrc.js
          phpcs.xml.dist
          package.json
          package-lock.json
          composer.lock
          AGENTS.md
          CLAUDE.md
          GEMINI.md
          EOF
          cd dist && zip -r "../isudev-library-${VERSION}.zip" isudev-library

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: v${{ steps.version.outputs.version }}
          files: isudev-library-${{ steps.version.outputs.version }}.zip
          generate_release_notes: true
